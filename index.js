// entry point
const dotenv = require('dotenv');

const path = require('path');
global.appRoot = path.resolve(__dirname);
// process.env.MASON_ROOT = path.join(appRoot, 'mason_packages');
// process.env.MASON_BUCKET = 'mason-binaries'

