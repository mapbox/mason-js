#!/usr/bin/env node
var loader = require('../lib/retrieve_package.js');
const log = require('npmlog');

loader.install.then((results) => {
  log.info('Mason Package Install Starting');
}).catch(error => log.error(error)); 